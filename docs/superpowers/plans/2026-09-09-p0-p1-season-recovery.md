# P0/P1 Season Recovery Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Restore the private race-day collection loop for the 2026/27 season, add checksum-backed local database backups, and run a token-efficient daily Codex巡检 at 08:00 HKT.

**Architecture:** HKJC public race cards and results continue to flow through the existing refresh/sync commands into the private local SQLite database. A finite ten-minute LaunchAgent captures only due T-30/T-10/T-3 market windows, while the daily Codex automation performs deterministic preflight, backup, settlement, coverage checks, and then advances exactly one research slice. Private data stays outside Git and cash remains `NO_BET`.

**Tech Stack:** Node.js CLI and tests, SQLite, macOS launchd, Codex cron automation, GitHub Actions/Pages.

---

### Task 1: Restore current-season collection

**Files:**
- Runtime: `$PROJECT_ROOT/hkjc-horse-model/data/hkjc.sqlite`
- Runtime: `$HOME/Library/LaunchAgents/com.superlaomiao.hkjc-race-day-cycle.plist`
- Runtime: `$HOME/Library/Logs/HKJC Local Horse Model/`

- [x] **Step 1:** Create a checksum-verified APFS backup of SQLite and both scheduler configurations.
- [x] **Step 2:** Refresh official September fixtures/results and sync them through the existing idempotent SQLite importer.
- [x] **Step 3:** Run one real due-window cycle before the first 2026-09-09 race and retain truthful `score-not-configured` status.
- [x] **Step 4:** Reproduce launchd exit 78, isolate log-path access as the failing boundary, move logs to the user Library, reinstall, and verify exit 0.
- [x] **Step 5:** Verify automatic cycles continue with exit 0, tighten the cadence to five minutes so T-3 cannot be skipped by alignment, and settle official results/available locks through deterministic preflight.

### Task 2: Add checksum-backed retained backups

**Files:**
- Modify: `package.json`
- Modify: `hkjc-horse-model/src/cli.js`
- Create: `hkjc-horse-model/src/database-backup.js`
- Create: `hkjc-horse-model/test/database-backup.test.js`
- Modify: `docs/operations/local-race-day-scheduler.md`

- [x] **Step 1:** Write failing tests for a SQLite snapshot, SHA-256 manifest, and bounded retention that never deletes unrelated files.
- [x] **Step 2:** Run the focused test and confirm the missing implementation fails.
- [x] **Step 3:** Implement `backup-db` using SQLite's consistent backup mechanism, an atomic manifest write, and a seven-backup default retention policy.
- [x] **Step 4:** Run focused tests, create a real backup manifest under private local storage, and feed it to `prospective-coverage`.
- [x] **Step 5:** Commit the tested backup slice with the P0/P1 recovery changes.

### Task 3: Restore a token-efficient daily巡检

**Files:**
- Runtime: `$CODEX_HOME/automations/hkjc-2/automation.toml`
- Modify: `docs/active-continuation-roadmap.md`

- [x] **Step 1:** Update the existing automation to run daily at 08:00 HKT with `gpt-5.6-sol` and high reasoning.
- [x] **Step 2:** Bound every run to 60 minutes: 5–10 minute deterministic preflight, 15–25 minute light non-race-day mode, or at most 55 minutes full race-day/research mode.
- [x] **Step 3:** Queue P2 scorer/decision wiring before P3 prospective collection/evaluation; advance one smallest safe unchecked slice per run.
- [x] **Step 4:** Reduce token use through compact machine-readable checks, focused file reads, no repeated broad web research, focused tests during development, and one full suite only after code changes.
- [x] **Step 5:** View the saved automation and verify schedule, model, status, workspace, safety boundaries, and task order.

### Task 4: Integrate and publish P0/P1

**Files:**
- Modify: `docs/active-continuation-roadmap.md`
- Modify: public operational summaries only when generated by the allowlisted publisher

- [x] **Step 1:** Record exact current-season counts, collector root cause/fix, backup status, P2/P3 queue, and cash `NO_BET` in the roadmap.
- [x] **Step 2:** Run focused tests, full `npm test`, `git diff --check`, public build, and privacy scan.
- [x] **Step 3:** Commit only code and public documentation from the fixed worktree; never add SQLite, raw snapshots, model artifacts, or private logs.
- [ ] **Step 4:** Push `codex/daily-hkjc`, fast-forward/merge safely into `main`, and verify GitHub Pages workflow and sanitized public page.
